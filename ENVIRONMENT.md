# إعداد متغيرات البيئة

هذه الواجهة لا تحتاج إلى بيانات قاعدة بيانات أو مفاتيح خادم. تُحفظ القيم الخاصة في إعدادات الأسرار الخاصة بالمشروع أو في ملف `.env` محلي غير مرفوع إلى Git عند تشغيل نسخة محلية من الكود.

| المتغير | مطلوب | الغرض | مثال للقيمة |
|---|---:|---|---|
| `VITE_ORB_API_BASE_URL` | نعم | عنوان واجهة ORB الخلفية التي تستقبل طلبات لوحة الأدمن. | `https://orb-production-2793.up.railway.app` |
| `VITE_GOOGLE_CLIENT_ID` | عند استخدام Google | Client ID العام لتشغيل زر تسجيل الدخول بحساب Google. | قيمة Google OAuth Client ID |
| `VITE_APP_ID` | مُدار من المنصة | معرّف تطبيق المنصة عند استخدام مصادقة المنصة. | يُضبط في إعدادات المشروع |
| `VITE_OAUTH_PORTAL_URL` | مُدار من المنصة | عنوان بوابة OAuth الخاصة بالمنصة. | يُضبط في إعدادات المشروع |

> لا تضعي أبداً `DB_URI` أو كلمة مرور MongoDB أو `JWT_SECRET_KEY` أو مفاتيح Paymob وCloudinary وFirebase في متغير يبدأ بـ`VITE_`؛ لأن هذه القيم تُضمّن في تطبيق المتصفح ويمكن للمستخدمين رؤيتها.

## طريقة الإعداد

في بيئة المشروع المنشورة، أضيفي أو عدّلي القيم من لوحة **Settings → Secrets**. أما عند تشغيل نسخة محلية خارج المنصة، أنشئي ملف `.env` خاصاً بجهازك، ضعي فيه الأسماء السابقة والقيم المطلوبة، واتركيه غير مرفوع إلى Git؛ فالريبو يستثني ملفات `.env` من التتبع. لا تُنشئي أو ترفعي ملفاً يتضمن قيماً حقيقية باسم `.env.example`.
